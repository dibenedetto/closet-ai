# URL base delle API ClosetAI. In dev lasciare commentato per usare il proxy Vite
# (/api → http://localhost:8000). In produzione impostare un URL completo, es.:
#   VITE_API_BASE_URL=https://closetai.example.com/api/v1
# VITE_API_BASE_URL=/api/v1
